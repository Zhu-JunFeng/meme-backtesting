# 项目协作约定

- 用户要求：每次完成代码修改并通过相关验证后，提交本次修改并 push 到当前项目远程分支，无需再次询问是否 push。
- Push 会触发自动发布；推送前检查是否有正在进行的发布，推送后检查 GitHub Actions 结果。发布失败必须明确告知，不将 push 成功等同于发布成功。
- 仅提交本次任务的相关修改，不夹带其他未确认的用户改动；不得强制推送或自动重跑历史回测。
