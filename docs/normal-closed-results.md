# 正常平仓查询与图表标记（2026-09-21）

## 查询约定

- 前端每次打开任务默认 `includeEndOfBacktest=false`；同一详情内切换 CA、池、分页保留开关。
- API 省略该参数仍按 `true` 兼容旧客户端。`false` 只保留完整的止盈、止损、动态锁盈、失效、超时订单。
- `classifyTrade` 是唯一查询态分类入口；`excluded_end` 仍只代表结束强平。未平仓和信息不完整分别提示，不把浮盈算入排除的已实现盈亏。
- 所有交易关联先稳定编号，再整笔过滤，再按时间、分页和标签筛选。无标签筛选时保留零交易 CA/池。
- 过滤曲线为初始资金加保留订单累计净盈亏，同一退出时间合并。不是原账户权益，不重新模拟资金占用。
- 非完成任务的未落库持仓数量不可用；已落库未平仓数不代表全部当前持仓。

## 标记约定

`partitionMarkers` 同时约束圆形标记与箭头：事件时间必须精确命中已加载行情，并在请求/可见窗口内。每次 getBars（含分页）登记时间，切换上下文清空集合，视野变化清除旧标记并版本隔离请求。缺失行情的事件保留列表，提示未绘制，不吸附邻近 K 线。

## 验证记录

- `TEST_DATABASE_URL` 指向本机隔离 PostgreSQL，完整 `pnpm test` 通过：前端 19、引擎 35、运行时 6、API 73 项。
- `pnpm build` 通过。保留既有前端大包 warning，本次不夹带打包重构。
- PostgreSQL 集成覆盖正常、结束强平、未平仓、信息不完整、费用、完整事件对、零交易 CA、标签交集、默认兼容、原报告不变。
- 分类单测覆盖五种正常退出、同根交易、零盈亏、非法时间/成交值/净盈亏；标记单测覆盖 30s/1m、同根多事件、缺失与未来时间、加载后出现。
- 浏览器 ROBIN 839：首笔视野约截至 15:05，只出现买1、卖1、买2、卖2、买3；后续事件不再堆叠在右侧。定位第5笔后买卖1至5恢复且每个标记精确命中对应行情。
- 发布前只读基线：批次 `fib-grid-20260920-v1` 已完成 1730 项、35 波，保留 20、清理 1710，无活动任务。已保存 32 份完成报告 SHA-256，发布后对照；原始基线位于忽略目录 `output/batch/normal-closed-baseline.json`。

本次不变更撮合、风险仓位算法、检查点、策略、冻结输入或批量排名；不重跑历史任务。发布后继续用 ROBIN 839 指定 CA 校验 12 笔正常退出、净盈亏 3246.175977893786，并复核报告校验值和 20 个保留任务。
