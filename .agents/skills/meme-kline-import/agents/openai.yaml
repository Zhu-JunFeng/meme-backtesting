interface:
  display_name: "Meme K线导入"
  short_description: "从信号明细同步项目信号与创建后24小时的30s及1m价格、市值K线"
  default_prompt: "Use $meme-kline-import to preview and import first-24-hour candles and project signals from this workbook."
policy:
  allow_implicit_invocation: true
