import type {BacktestConfig,Candle,SymbolRef} from '@meme/domain';
export const symbols:SymbolRef[]=[{chain:'sol',ca:'a',pairId:'a'},{chain:'sol',ca:'b',pairId:'b'}];
export const configuration=():BacktestConfig=>({name:'test',symbols,interval:'30s',valueType:'mcap',impulseCondition:{type:'impulse_fractal_swing',leftBars:2,rightBars:2,lookbackBars:30,minGainPercent:10,maxDurationBars:20,requireVolumeExpansion:false},entryConditionGroup:{mode:'all',conditions:[{type:'fib_retracement',zoneLow:.2,zoneHigh:.8}]},invalidationConditionGroup:{enabled:false,mode:'any',conditions:[]},exitConfig:{stopLoss:{type:'percent',value:10},takeProfit:{type:'risk_reward',ratio:2},closeAtEnd:true},executionConfig:{initialCapital:1000,feePercent:1,slippagePercent:1,buyTaxPercent:1,sellTaxPercent:1,fillMode:'current_bar_close'},positionConfig:{mode:'pyramiding',maxEntries:3,maxConcurrentPositions:2,allowReentry:true,sizing:{type:'fixed_percent',value:10}}});
export function candles(n=800,offset=0):Candle[]{return Array.from({length:n},(_,i)=>{const close=10+Math.sin(i*.42)*3+Math.sin(i*.71);return {time:(i+offset)*30000,closeTime:(i+offset+1)*30000,open:close*(1+Math.cos(i)*.015),high:close*1.05,low:close*.95,close,volume:100+(i%11)*20};}).filter((_,i)=>i%31!==0);}
